const arr = [5, 6, 7, 8, 9];
const arr1 = [5, 6, 7, 8, 9];

const newArr = arr;  // Passing the address of the memory.

console.log(newArr);
console.log(newArr === arr);
console.log(arr == arr1);
console.log(arr == arr1);